import { someEvent, anotherEvent } from 'effector/ex/events';

// use your apis here if your events are effects
const useApi = (): void => {
  // someEvent.use(SomeApi.someEvent);
  // anotherEvent.use(SomeApi.anotherEvent);
};

export default useApi;
